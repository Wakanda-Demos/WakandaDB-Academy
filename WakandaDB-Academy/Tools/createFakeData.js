// Uncomment the appropriate lines before launching it. They are commented to avoid// running the file by mystake (and think about reßcommented them after execution ;-))// theLog is dump to screen at the end of the filevar	COUNT_EMPLOYEES_TO_CREATE,	startDate,	endDate,	duration,	generator;generator = require("fakedata/generator");startDate = new Date();// a good number would be 500,000//COUNT_EMPLOYEES_TO_CREATE = 500000;COUNT_EMPLOYEES_TO_CREATE = 20000;console.info("Start of log: ", startDate);console.info(" ::::: Creating ", COUNT_EMPLOYEES_TO_CREATE, " Employees  Companies...");loginByPassword('admin', 'devtest');generator.buildFakeData(COUNT_EMPLOYEES_TO_CREATE, {log:false});ds.flushCache();console.info(" End creating Employees. Employees: ", ds.Employee.length, ", Companies: ", ds.Company.length, ", Country: ", ds.Country.length, " ::::: ");endDate = new Date();duration = endDate - startDate;console.info("DURATION: ", duration, " ms");duration = String(duration);// --EOF--